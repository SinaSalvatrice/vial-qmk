#pragma once

// The custom keymap defines 9 layers (0-8); override the keyboard-level
// DYNAMIC_KEYMAP_LAYER_COUNT so VIA exposes all layers including the
// layer-select layer at index 8.
#define DYNAMIC_KEYMAP_LAYER_COUNT 9
